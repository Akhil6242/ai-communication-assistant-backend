# Server Configuration - Cloud Run uses PORT environment variable
server.port=${PORT:8080}

# Database Configuration - Your Google Cloud SQL database
spring.datasource.url=jdbc:postgresql://136.115.59.195:5432/ai_communication_db
spring.datasource.username=postgres
spring.datasource.password=Aiemail@gr8
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# CORS Configuration - Allow your frontend
cors.allowed-origins=*

# Health check for Cloud Run
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
management.server.port=8080

# Logging
logging.level.com.emailassistant=INFO
